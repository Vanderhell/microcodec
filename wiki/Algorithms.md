# Algorithms

`microcodec` includes five focused codecs:

- RLE
- Varint
- Delta
- LZSS
- Huffman

Each codec is intended for a specific embedded data pattern rather than trying
to be an all-purpose compression layer.
